# TODO:
 - properly configure .env
 - local development?
  - validate .dockerignore works
  - create local shell run scripts
 - add unit tests. Compare rust to python inputs/outputs
 - add c code
 - use with helm and k3d?
 - handle edge cases with rabbitmq. boss failing, worker failing. timeouts
 - config and .env