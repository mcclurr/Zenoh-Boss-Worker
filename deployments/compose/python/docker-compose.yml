services:
  zenoh:
    image: eclipse/zenoh
    hostname: zenoh
    ports:
      - "7447:7447"
      - "8000:8000"
    environment:
      - RUST_LOG=info

  orchestrator:
    build:
      context: ./../../..
      dockerfile: docker/Dockerfile.python-app
    container_name: orchestrator
    depends_on:
      - zenoh
    command: ["python", "-u", "-m", "bw.services.orchestrator.main"]
    volumes:
      - ./../../../out/python:/app/out
    env_file:
     - ./../../../.env
    environment:
      - ZENOH_ENDPOINT=tcp/zenoh:7447

  worker1:
    build:
      context: ./../../..
      dockerfile: docker/Dockerfile.python-app
    container_name: worker1
    command: ["python", "-u", "-m", "bw.services.worker.main"]
    volumes:
      - ./../../../out/python:/app/out
    env_file:
     - ./../../../.env
    environment:
      - WORKER_NAME=worker-python-1

  worker2:
    build:
      context: ./../../..
      dockerfile: docker/Dockerfile.python-app
    container_name: worker2
    command: ["python", "-u", "-m", "bw.services.worker.main"]
    volumes:
      - ./../../../out/python:/app/out
    env_file:
     - ./../../../.env
    environment:
      - WORKER_NAME=worker-python-2

  producer:
    build:
      context: ./../../..
      dockerfile: docker/Dockerfile.python-app
    container_name: producer
    depends_on:
      - zenoh
    command: ["python", "-u", "-m", "bw.services.producer.main"]
    volumes:
      - ./../../../out/python:/app/out
    env_file:
     - ./../../../.env
    environment:
      - ZENOH_ENDPOINT=tcp/zenoh:7447

  consumer:
    build:
      context: ./../../..
      dockerfile: docker/Dockerfile.python-app
    container_name: consumer
    depends_on:
      - zenoh
    command: ["python", "-u", "-m", "bw.services.consumer.main"]
    volumes:
      - ./../../../out/python:/app/out
    env_file:
     - ./../../../.env
    environment:
      - ZENOH_ENDPOINT=tcp/zenoh:7447